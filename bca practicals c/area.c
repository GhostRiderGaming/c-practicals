#include<stdio.h>
int main(){
    int area,side;
    printf("Enter the value of side of square:");
    scanf("%d",&side);
    area = side*side;
    printf("Area of Square is %d",area);
    return 0;
}